https://docs.docker.com/engine/install/debian/

orbctl delete debian && orbctl create -a arm64 debian:bookworm debian -u roel -c cloud-init-yaml && ssh root@debian@orb
orbctl delete debian && orbctl create -a arm64 debian:bookworm debian -u roel -c cloud-init-yaml

orbctl delete debian
orbctl list
orbctl create -a arm64 debian:bookworm debian -u roel -c cloud-init-yaml
ssh debian@orb


cloud-init status
tail /var/log/cloud-init.log
tail /var/log/cloud-init-output.log



docker build --platform linux/amd64 -t dfmsite:latest .

docker image save dfmsite:latest -o dfmsite.tar
docker image load -i dfmsite.tar

docker save dfmsite:latest | bzip2 | pv | ssh root@www-dinxperfm-nl.toffe.site docker load
